import React, { Component } from 'react';
import { View, ScrollView } from 'react-native';
import { Container, Content, Thumbnail } from 'native-base';

export class NetAvatar extends Component {
	render() {
		return <View />;
	}
}
